<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
</head>
<body>
    <h1>Photo Locations</h1>
    <ul>
        <% if (photos && photos.length > 0) { %>
            <% photos.forEach(photo => { %>
                <li>
                    <img src="/image/<%= photo.filename %>" alt="<%= photo.filename %>" style="width: 200px; height: auto;"/>
                    <a href="/location?address=<%= encodeURIComponent(photo.address) %>">
                        <%= photo.address || 'No address' %>
                    </a>
                </li>
            <% }) %>
        <% } else { %>
            <p>No photos found.</p>
        <% } %>
    </ul>
</body>
</html>
