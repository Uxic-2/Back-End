<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Liked Places</title>
</head>
<body>
    <h1>좋아요 한 장소</h1>

    <ul>
        <% if (likedPlaces.length > 0) { %>
          <% likedPlaces.forEach(function(place) { %>
            <li>
              <strong>Place Name:</strong> <%= place.place_name %><br>
              <strong>Address:</strong> <%= place.address_name %><br>
              <strong>Phone:</strong> <%= place.phone %>
            </li>
          <% }) %>
        <% } else { %>
          <li>No liked places found.</li>
        <% } %>
      </ul>
</body>
</html>
