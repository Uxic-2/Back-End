<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>검색 결과</title>
    <link rel="stylesheet" href="/styles/search.css">
    <script>
        var userId = '<%= userId %>';
        var likedPhotoIds = JSON.parse('<%= JSON.stringify(likedPhotoIds) %>'); // 문자열로 전달된 likedPhotoIds를 배열로 변환

        function handleCheckboxChange(photoId, isChecked, element) {
    const url = isChecked ? '/photos/add' : '/photos/remove';
    const data = { photoId: photoId }; // action은 URL에서 처리하므로 제거
    

    fetch(url, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify(data),
    })
    .then(response => response.json())
    .then(result => {
        if (result.message) {
            const likesElement = element.closest('.card-meta').querySelector('.photo-likes');
            const currentLikes = parseInt(likesElement.textContent, 10);
            likesElement.textContent = isChecked ? currentLikes + 1 : currentLikes - 1;

                  // 좋아요 상태를 변경했으므로 likedPhotoIds 배열을 업데이트
                  if (isChecked) {
                    likedPhotoIds.push(photoId); // 좋아요 추가 시 배열에 photoId 추가
                } else {
                    likedPhotoIds = likedPhotoIds.filter(id => id !== photoId); // 좋아요 해제 시 배열에서 photoId 제거
                }
            }
        })
        
    .catch(error => console.error('Error:', error));
}
        function isLiked(photoId) {
            return likedPhotoIds.includes(photoId);
        }
    </script>
</head>
<body>
    <h1>검색 결과</h1>
    <div class="container">
        <% photos.forEach(function(photo) { %>
            <div class="card">
                <img src="/image/<%= photo.filename %>" alt="<%= photo.filename %>" class="card-img" onclick="window.location.href='/location?address=<%= encodeURIComponent(photo.address) %>&latitude=<%= photo.gps.latitude %>&longitude=<%= photo.gps.longitude %>'">
                <div class="card-body">
                    <h5 class="card-title"><%= photo.filename %></h5>
                    <p class="card-text">
                        주소: <%= photo.address %>
                    </p>
                    <p class="card-text">
                        설명: <%= photo.description || 'No description' %>
                    </p>
                    <div class="card-meta">
                        <p>업로드 시간: <%= photo.uploadDate %></p>
                        <p>좋아요 수: <span class="photo-likes"><%= photo.likes %></span></p> <!-- 좋아요 수 -->
                        <p>타임스탬프: <%= photo.timestamp %></p>
                        <p>GPS: 위도 <%= photo.gps.latitude %>, 경도 <%= photo.gps.longitude %></p>
                        <label>
                            <input 
                                type="checkbox" 
                                checkbox.checked = likedPlaceIds.includes(place.id);
                                onchange="handleCheckboxChange('<%= photo._id.toString() %>', this.checked, this)" 
                                <% if (likedPhotoIds.includes(photo._id.toString())) { %>checked<% } %>> <!-- 좋아요 상태에 따라 체크 여부 결정 -->
                            좋아요
                        </label>
                    </div>
                </div>
            </div>
        <% }); %>
        
    </div>
</body>
</html>
